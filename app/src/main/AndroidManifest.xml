<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitCore">

        <!-- 登录页 -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主页面 -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- 健身计划 -->
        <activity
            android:name=".activity.PlanListActivity"
            android:exported="false" />

        <activity
            android:name=".activity.WorkoutDetailActivity"
            android:exported="false" />

        <!-- 运动记录 -->
        <activity
            android:name=".activity.WorkoutTimerActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activity.WorkoutHistoryActivity"
            android:exported="false" />

        <!-- 编辑资料 -->
        <activity
            android:name=".activity.EditProfileActivity"
            android:exported="false" />

        <!-- 消耗计算 -->
        <activity
            android:name=".activity.CalorieGuideActivity"
            android:exported="false" />

        <!-- BMI指南 -->
        <activity
            android:name=".activity.BMIGuideActivity"
            android:exported="false" />

        <!-- 账号信息 -->
        <activity
            android:name=".activity.AccountInfoActivity"
            android:exported="false" />

        <!-- 提醒设置 -->
        <activity
            android:name=".activity.ReminderSettingsActivity"
            android:exported="false" />

        <!-- 通知接收器 -->
        <receiver
            android:name=".utils.NotificationHelper$ReminderReceiver"
            android:exported="false" />

    </application>
</manifest>
